import React, {FC, useState} from 'react';
import {
  Image,
  StyleSheet,
  Text,
  View,
  SafeAreaView,
  TouchableOpacity,
} from 'react-native';

const Indicator: FC = () => {
  return <Text>This is indicator</Text>;
};

export default Indicator;
