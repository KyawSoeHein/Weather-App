import React, {FC} from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Home: FC = () => {
  return (
    <View>
      <View
        style={{backgroundColor: 'transparent', height: '50%', width: '100%'}}
      />
      <Text>Hello</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Home;
